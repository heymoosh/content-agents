title = "content-agents gitleaks config"

[extend]
useDefault = true

[[allowlists]]
description = "Verified false positive: LinkedIn's own public login URL, not a credential"
regexes = [
  '''https://www\.linkedin\.com/login''',
]
