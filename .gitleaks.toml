title = "content-agents gitleaks config"

[extend]
useDefault = true

[[allowlists]]
description = "Verified false positive: src/pull/platforms/linkedin.ts only holds LinkedIn's own public URLs (login + analytics pages), not credentials -- full file checked 2026-07-20, no other secret-shaped content"
paths = [
  '''src/pull/platforms/linkedin\.ts''',
]

[[allowlists]]
description = "Verified false positive: the linkedin-client-secret rule matches the TYPE ANNOTATION in `export const linkedin: PatternCollector = {`, not a credential. PatternCollector is an interface imported from ./shared.js. Full 278-line file checked 2026-08-23; the collector reads public profile pages through a Playwright session and holds no keys."
paths = [
  '''src/patterns/collectors/linkedin\.ts''',
]

[[allowlists]]
description = "Verified historical false positives: this retained review artifact contains only deterministic SHA-256 artifact/cohort fingerprints. Current file and flagged commit 15b76c6 were read in full on 2026-08-30; neither contains a credential."
paths = [
  '''docs/content-studio-program/runs/pattern-review-staging-20260825/review-staging-result\.json''',
]

[[allowlists]]
description = "Verified historical false positive: this retained decision artifact contains a deterministic SHA-256 candidate-cohort fingerprint. Current file and flagged commit d091f62 were read in full on 2026-08-30; neither contains a credential."
paths = [
  '''docs/content-studio-program/runs/pattern-evidence-gate-20260825/candidate-account-slate-decision\.json''',
]
