# Extracts — We tend to think that AI training has nothing to do with culture, beliefs, or values

Pillar: human-ai (AI training/reward design as a values decision, critique of the "AI is neutral
tech" assumption)

Source kind: substack-note (source_kind: substack-note) — the whole note is the extract per
notes-mode.md; quote card uses the full body, not a sub-sentence.

Source triage: frame-native (a testable belief worth the LinkedIn/X treatment — no beat-2 quoted
belief-holder, no anonymize-able third-party case, so LinkedIn/X spin falls back to normal
non-case framing rather than the case-skeleton beats).

## Most quotable lines

- L10: "We tend to think that AI training has nothing to do with culture, beliefs, or values."
  (the hook — names the unexamined assumption)
- L10: "But when it comes to deciding how to reward an AI model for its behavior, the values that
  you bring to that decision make all of the difference." (the claim)
- L12: "I explain a bit more below." (note-native pointer line, not quotable on its own)

## Whole-note quote (for the card, per notes-mode.md)

Both L10 sentences together fit under 280 chars, but the note is only two sentences total
(assumption, then reveal) — using both on the card would leave nothing distinct for the
per-platform context caption without literally repeating the image text. So the card takes only
the first sentence (the assumption, 85 chars, a complete thought on its own): "We tend to think
that AI training has nothing to do with culture, beliefs, or values." The second sentence (the
reveal, verbatim) becomes each caption's context — genuinely distinct material, not a repeat.
The trailing "I explain a bit more below." (L12) is a note-native pointer, not part of the
quotable claim, dropped from both.
