# Routing — civic-tech + human-ai — 2026-07-04

Generated by `npm run route` from analytics + config/routing.yaml, merged across 2 pillars in one pass: a platform is `include` if ANY pillar includes it, UNLESS any pillar's editorial `never` rule vetoes it (that veto wins regardless of other pillars). Only `include` platforms are atomized and queued; Muxin's review-queue approval stays the final gate. `npm run validate` hard-fails any derivative drafted for a platform marked `skip` here.

> Note: this run's analytics.db is the thin copy in this git worktree, not the main checkout's real accumulated data — every platform came back cold-start instead of the real data-driven scores the 2026-06-24 brief already computed on the full dataset. The `linkedin` row below is corrected by hand per that brief (see below); if you re-run this from the main checkout once it has real data, `x`/`bluesky` rows may come back data-driven instead of cold-start.

| platform | decision | fit | confidence | why |
|---|---|---|---|---|
| x | include | — | cold-start | civic-tech: cold-start (no tagged data yet) — posting broadly to gather signal; human-ai: cold-start (no tagged data yet) — posting broadly to gather signal |
| linkedin | skip | — | brief | router says cold-start include, but the 2026-06-24 brief found human-ai on LinkedIn a weak fit (atomized 2.0 vs organic 9.6 — wrong topic for this room) and says to treat it as skip by hand until the router itself catches up on more data |
| bluesky | include | — | cold-start | civic-tech: cold-start (no tagged data yet) — posting broadly to gather signal; human-ai: cold-start (no tagged data yet) — posting broadly to gather signal |
| community:democratic-resilience | include | — | rule | civic-tech: editorial rule: always route here |
| quote-card | include | — | always | civic-tech: format asset — always generated; human-ai: format asset — always generated |

from_brief: briefs/2026-06-24-strategy-brief.md
directives_applied: [do_less:human-ai-to-linkedin]
