# SLICE-5H: separate a configured quote card's on-image quote from its post text

Protocol: `AGENTS.md` → `## Slice protocol`. Read that and this file only.
Do not open `docs/content-studio-master-status.md` unless a heading is cited below.
Do not load the repository for context. Do not commit.

## Goal

A quote-card variant generated by the Content Studio's configured path ends up with **two distinct
texts** instead of one string doing both jobs:

- `derivatives/<id>.md` — the **post text**: the per-platform context that frames the quote. This is
  what the review-queue row displays and what `publish:cards` ships as the post body alongside the
  image. Unchanged file path, unchanged consumers.
- `derivatives/<id>-quote.md` — a **new companion definition derivative**: the short verbatim quote
  that gets drawn ON the image. `platform: quote-card`, `source_lines` naming the quote's own
  source line(s), no spin, body ≤180 characters, body only (no frontmatter noise beyond that).

The configured media plan for a card renders from the **quote** file, not the post body.

Today one drafted string is the derivative body, the render plan's `sourceText`, AND the text
painted onto the card (`jobs.ts:1184` → `configuredMediaPlan`, `configured-media.ts:125` →
`renderStill`, `render.ts:60`). The consequence is that a Studio card either paints a full platform
post onto the image or leaves a bare quote as the post body. Either way the quote ships without
context, which is the failure `/atomize` step 7 was built to prevent (Muxin, 2026-07-03: a card is a
bare-quote image "with its OWN per-platform CONTEXT caption — so the quote never ships alone, out of
context"). This slice ports that separation onto the configured path.

Also fix the drafting blind spot this exposed: initial configured drafting gives a card variant no
card-specific instruction at all. `configuredTreatmentInstruction` (`jobs.ts:349-362`) knows only
generic treatment keys. The only card awareness in the repo is `isCardCaption` in the REVISE path
(`jobs.ts:129,150`), which (a) applies only to editing an existing file and (b) uses the regex
`^quote-card-\d+-[a-z]+$`, which does not match a configured variant id. A card variant's post text
must be drafted as context, under the same instruction the revise path already uses: give the setup,
mechanism or stakes around the quote, and never restate the quote that is on the image.

## Difficulty

hard — it changes what configured generation composes (a second drafted artifact per card variant,
with its own extraction-first obligation) and re-points the render plan. Prose-composing behavior,
so it carries the stronger content-generation gates.

## Depends on

None. 5F is accepted and unrelated. This slice reads nothing 5F wrote.

## Owned files

Parallel-safe: **no** — single lane. The plan shape flows through all four files in one chain
(drafting writes the quote file → the plan carries it → the runtime passes it to the renderer), so
splitting the lanes would put two workers on the same contract mid-change. Serial is the safe
fallback and the protocol requires no justification beyond this line.

### Lane A — separate the two texts end to end

- `src/review/jobs.ts`
- `src/review/jobs.test.ts`
- `src/review/configured-media.ts`
- `src/review/configured-media.test.ts`
- `src/review/configured-media-runtime.ts`
- `src/review/configured-media-runtime.test.ts`

## Do not touch

- `src/video/render.ts` — `renderStill` already reads `derivatives/<quoteName>.md` from whatever
  name it is handed (`render.ts:60`). Hand it the quote file's name; do not change how it reads.
- `src/review/rows.ts` — `enrich()` loads the row's displayed body from `derivatives/<id>.md`
  (`rows.ts:244-251`). That file keeps holding the post text, so this stays correct with no edit.
  If you believe it needs changing, stop and report that instead of changing it.
- `src/publish/cards.ts` — the publish side already treats the derivative body as the post caption
  (`cards.ts:61-64`). It is correct as written and out of scope.
- `src/review/content-request.ts` — variant identity and the one-image-per-platform fan-out question
  are a SEPARATE later slice, explicitly out of scope here. Do not add fan-out, do not deduplicate
  renders, do not change `variantId`.
- The `/atomize` CLI path (`src/atomize/**`, `.claude/skills/atomize/`) — it already does this
  correctly and is not being changed.
- Any treatment key, prompt, or behavior for non-card variants. A text variant's drafting must come
  out byte-identical to today.

## Cited headings

`docs/content-studio-master-status.md` → `## Standing constraints`

## Acceptance

- [ ] For a configured variant whose `media` is `static-quote-card` or `animated-quote-card`,
      generation writes BOTH `derivatives/<id>.md` (post text) and `derivatives/<id>-quote.md`
      (on-image quote). For every other media value, exactly the files written today are written,
      with byte-identical content for the same inputs.
- [ ] The quote file carries `platform: quote-card`, a `source_lines` list, no `spin`/`angle`, and a
      body of at most 180 characters.
- [ ] The quote body is **verbatim from the source** — extraction-first, not composed. The post text
      may be treated/spun within the existing configured-treatment guardrails; the quote may not.
- [ ] The card's media plan `sourceText` is the QUOTE file's body, not the post body, so the
      renderer paints the quote. Proven by asserting the plan/stage content, not by asserting that a
      function was called.
- [ ] The runtime's card branch passes the quote file's name to the renderer
      (`configured-media-runtime.ts:307-311`), so `renderStill` reads `derivatives/<id>-quote.md`.
- [ ] A card variant's POST text is drafted as context: the drafting instruction tells the model to
      give the setup, mechanism or stakes around the quote and never to restate the quote. The
      existing revise-path wording (`jobs.ts:150`) is the reference; do not invent a second
      vocabulary for the same idea.
- [ ] `isCardCaption` (`jobs.ts:129`) recognizes a configured card variant id, or the card-awareness
      is routed some other way that a test proves. Today's regex matches only legacy
      `quote-card-N-<target>` ids; a Studio id must not silently fall through to the generic path.
- [ ] No em dashes and no AI tells in anything composed, per `config/voice.yaml`.
- [ ] Nothing publishes and no review status changes as a result of this slice.

## Verify

Focused, from the repo root:

```
node --import tsx --test src/review/configured-media.test.ts src/review/configured-media-runtime.test.ts
```

```
node --import tsx --test src/review/jobs.test.ts
```

Then type-check:

```
npx tsc --noEmit
```

## Observable result

Generate a quote card for one platform from the Content room. The review-queue row shows a real
post: the context that frames the quote, and it does not repeat the quote. The rendered
`images/<id>.png` shows the short verbatim quote and not the post text. `derivatives/<id>-quote.md`
exists beside `derivatives/<id>.md`, and its body is a line traceable to the source.

Generate any non-card variant. Its derivative is exactly what it is today.

## Risk

medium — audit required: **yes, cross-family**. This changes what the system composes: it adds a
second drafted artifact per card variant carrying its own extraction-first obligation, and it
re-points what gets painted onto a published image. Per the root `CLAUDE.md` rule 7, content
generation logic takes the stronger test and audit gates. The blast radius is bounded (card media
only; every other media path must be byte-identical), and nothing here publishes.

## Families

- Builder: Claude (strong tier) — single lane. The work is prompt and composed-artifact judgment,
  which is where Claude is the better one-shot fit.
- Auditor: Codex / GPT (strong tier) — different family from the builder, per the protocol's
  cross-family rule. Give it the acceptance list, the diff, the changed-file list and the focused
  check output; not the master document and not the worker transcript.

## Closeout

Repo-wide gate, run UNSANDBOXED (the sandbox produces roughly 196 phantom venture failures):

```
npm run check
```

## Deviations accepted by the coordinator

**The rendered image is named after the quote companion, not the variant id:**
`images/<id>-quote.png` (and `.mp4`), where this packet's `## Observable result` said
`images/<id>.png`. `renderStill` names its output after the derivative it is handed
(`src/video/render.ts:64`) and that file is on the do-not-touch list, so pointing the renderer at
the quote companion necessarily renames the image. The builder updated both the stage record's
`outputPath` and the runtime's `primaryAsset`/companion. Audited and cleared: the auditor traced
`renderStill`, checkpoint/promotion and the publish path and confirmed nothing reconstructs the old
name (the queue row's asset is `media-stages/<id>.json`, and card copy is keyed by `row.id`).
Coordinator-verified independently: no remaining constructor of `images/${variant.identity.id}`
exists in `src/`.

## Audit record

Cross-family audit: **Codex / GPT** (builder was Claude), given the acceptance list, the candidate
diff, the changed-file list and the focused check output — not the master document, not the worker
transcript. Three established defects were converted into builder checklist items and closed with
evidence:

1. **The render input was unbound to approval.** The approval digest covers the stage plan, but
   `renderStill` reads the quote off disk, so the companion was a second render input the digest
   never saw: editing it after approval would paint unapproved, possibly non-verbatim, possibly
   over-length text onto the card, defeating the extraction-first guarantee this slice exists to
   provide. Closed by `assertApprovedCardQuoteOnDisk`
   (`src/review/configured-media-runtime.ts:331-352`, called at `:361`), which compares the
   companion body against the approved plan's `sourceText` before anything is spawned and refuses on
   mismatch. Proven by mutating the companion after approval and asserting the render is refused, no
   `images/` is created, and the stage stays `approved` with nothing promoted
   (`configured-media-runtime.test.ts:78-100`).
2. **Cards generated before this slice would be stranded.** Occupancy treated row + post derivative
   + stage as complete, so a pre-existing card returned `existing: true` while rendering failed on
   the missing companion, unrepairable. Closed by adding a fourth `quote` occupancy key for card
   media only and a backfill (`repairConfiguredCardQuotes`, `jobs.ts:1013-1049`) that extracts the
   quote from the source lines the post derivative already recorded — no model call, no new
   authorization boundary — then re-points the stage plan and **drops the stale approval** so a
   changed render input must be reviewed again. Stages already `rendered` or `promotion-pending` are
   skipped, leaving finished assets and their digests intact. The human-reviewed post derivative is
   never rewritten (asserted byte-for-byte).
3. **The byte-identity criterion was under-tested.** It had been checked for `media: "none"` only,
   on selected fields rather than bytes. Closed by capturing a real pre-change baseline (stashing
   the three implementation files) across all five non-card media values and comparing every written
   byte of both derivatives, both media stages and the review queue.

Declined as scope creep, recorded so they are not relitigated: rewriting sentence splitting to
handle decimals and abbreviations, and re-documenting untreated-control behavior.

## Found during this slice, NOT fixed here — needs its own slice

**Studio-generated cards cannot reach the card publisher.** Configured card rows keep
`platform: "linkedin"` / `"x"`, but `publish:cards` selects only `quote-card` platforms
(`src/publish/cards.ts:78`), so a card generated in the Content room never enters that publisher.
Pre-existing, unrelated to this change, and deliberately out of scope — but it means 5H makes the
card's post text correct without yet making the card publishable from Studio. Pair it with the
other deferred card item: configured generation renders a SEPARATE image per platform
(`jobs.ts` output path is keyed per variant id) instead of sharing one card across platforms the
way `/atomize` does.

## RESULT BLOCK

- Changed paths: `src/review/jobs.ts` (+188/-14), `src/review/jobs.test.ts` (+379/-1),
  `src/review/configured-media.ts` (+30/-1), `src/review/configured-media.test.ts` (+22/-0),
  `src/review/configured-media-runtime.ts` (+61/-5),
  `src/review/configured-media-runtime.test.ts` (+66/-5). No do-not-touch file changed.
- Outcome: a configured card variant now writes two texts. `derivatives/<id>.md` keeps the post
  text (unchanged path, unchanged consumers: the review row and the publish caption) and a new
  `derivatives/<id>-quote.md` holds the on-image quote — `platform: quote-card`, `source_lines`, no
  spin, gated against the shared `config/platforms.yaml` quote-card limit of 180 alongside every
  other platform limit, so one overrun aborts the whole set atomically. The quote is extracted
  verbatim and deterministically from the variant's OWN approved source lines (longest fitting
  sentence, earliest on a tie; an all-overrunning source is cut at a word boundary, never
  rewritten), never from the treated body. The media plan's `sourceText` is the quote, and the
  runtime hands `--quote <id>-quote` to `renderStill`, so the image carries the quote and the post
  carries the context. Card post text is drafted as context at initial generation under one shared
  `CARD_CONTEXT_RULE` used by both the drafting prompt and the revise path, and
  `isConfiguredCardVariantId` gives base64url Studio ids the card awareness the legacy regex could
  never match. Every non-card media path writes byte-identical files.
- Checks run and results: focused —
  `node --import tsx --test src/review/configured-media.test.ts src/review/configured-media-runtime.test.ts`
  and `node --import tsx --test src/review/jobs.test.ts`, both exit 0; `npx tsc --noEmit` exit 0.
  Byte baseline for the five non-card media values captured against the stashed pre-slice
  implementation and diffed clean (16991 bytes each), then pinned as per-file SHA-256.
  Repo-wide gate `npm run check` UNSANDBOXED: **exit 0, 4204 pass / 485 suites / 0 fail / 0 skip**
  (+19 tests vs 5F's 4185/485/0, same suite count — exactly the new blocks). The three
  `serve.test.ts`-family failures the builder saw when running those files in isolation do not
  occur in the full gate.
- Evidence locations: quote extraction `src/review/jobs.ts:390-435`; pre-write computation and the
  limit gate `:1229-1250`; companion write and plan re-point `:1277-1296`; stage record
  `quoteDerivativePath`/`outputPath` `:995-1006`; shared card rule and Studio-id awareness
  `:126-152`, drafting instruction `:368-386` injected at `:482`; repair `:1013-1049` with
  occupancy `:1067-1080`; approval binding `src/review/configured-media-runtime.ts:331-352` called
  at `:361`; renderer argv and assets `:299-327`.
- Unresolved: none inside this slice. Two card items are deliberately deferred to their own slices
  and recorded under `## Found during this slice` above: Studio cards do not reach `publish:cards`,
  and configured generation renders one image per platform instead of sharing a card.

## Closeout result

**ACCEPTED — merged to local `main`** (2026-09-05). Cross-family audit obtained (Codex vs. a Claude
builder), three established defects converted to checklist items and each closed with an
outcome-asserting test — see `## Audit record`. Coordinator-verified the final diff against this
packet, including the one deviation (`## Deviations accepted by the coordinator`) and an independent
check that no constructor of the old `images/<id>` name survives anywhere in `src/`. Gate run once,
last, unsandboxed: exit 0, 4204/485/0/0.

This closes the §5 port. Five capabilities ported, three declined (scoring/soft gate, home-brand
thread-check, strategy-brief directives), one shipped here. **Item `3b`** — retire `/cycle`'s
drafting step — is now unblocked, and it takes the stronger verification because it removes a
drafting path. Note before scheduling it: the two deferred card items above mean a Studio card's
copy is now correct but the card still cannot publish from Studio, so weigh whether `3b` should
wait on the publish-routing fix.
